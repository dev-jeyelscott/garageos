# GarageOS Web PWA environment example
# Copy this file to apps/web/.env.local for local development.
# Do not commit real .env.local files.

NODE_ENV=development

# Public browser-safe values only.
NEXT_PUBLIC_APP_NAME=GarageOS
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1